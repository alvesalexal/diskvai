<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diskvai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CadastroEntregador"></activity>
        <activity android:name=".PrincipalEntr" />
        <activity android:name=".PrincipalEmp" />
        <activity android:name=".PrincipalCli" />
        <activity android:name=".CadastroEmpresa" />
        <activity android:name=".telaTeste" />
        <activity android:name=".CadastroCliente" />
        <activity android:name=".TipoCadastro" />
        <activity android:name=".Activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>